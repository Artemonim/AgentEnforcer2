# Agent Enforcer 2: Концепт локального CI

[![English](https://img.shields.io/badge/lang-English-lightgrey.svg)](README.md)
[![Russian](https://img.shields.io/badge/lang-Русский-blue.svg)](README.ru.md)

**Эталонная архитектура для создания надёжных, независимых от языка систем локального CI.**

Agent Enforcer 2 — это концепция для реализации локального CI в ваших проектах. Он документирует проверенные в бою паттерны для оркестрации проверок качества кода, управления кэшированием, обнаружения зависаний и создания структурированных отчётов.

**Смотрите также**:

<a href="https://github.com/Artemonim/AgentCompass">
      <img src="https://github-readme-stats.vercel.app/api/pin/?username=Artemonim&repo=AgentCompass&theme=transparent&hide_border=true&title_color=2f80ed&text_color=ffffff&icon_color=2f80ed" />
    </a>
    <br>

## С чего начать

### Для вайбкодеров

- Используйте наиболее мощную доступную вам LLM-модель (согласно актуальным бенчмаркам вроде ARC-AGI-2, HLE, MRCR v2, Terminal-Bench 2.0 или их современным преемникам).
- Этот репозиторий предназначен для передачи LLM в качестве контекста. Скопируйте промпт ниже для чата с AI-агентом.
- Делайте это в два последовательных чата:
    - Чат 1: планирование — [создать CI_TODO.md](#чат-1).
    - Чат 2: реализация — [реализовать CI_TODO.md](#чат-2).

#### Чат 1

> Чтобы Cursor (v2.4.21) эффективно обработал ваш запрос:
> 1. Вручную введите `@Browser` вместо `INSERT_BROWSER` и нажмите `ENTER`
> 2. Вставьте `https://github.com/Artemonim/AgentEnforcer2/blob/master/README.ru.md` вместо `README_LINK`
> 3. Если в проекте уже есть документация `README.md`, `TODO.md`, `AGENTS.md` или иная подобная - укажите её перед шаблоном промпта.

```
Создай `CI_TODO.md` для системы локального CI в моём проекте, следуя схеме Agent Enforcer 2.

Через INSERT_BROWSER изучи и следуй документации начиная с README_LINK. GitHub-доки читай в raw (`raw.githubusercontent.com` / кнопка `Raw`).

- Адаптируй паттерны под язык моего проекта и существующий инструментарий. НЕ копируй файлы дословно — создавай реализации, адаптированные под мою кодовую базу.
- По умолчанию выбери и зафиксируй полный рекомендуемый набор проверок качества для стека проекта (fmt/lint/typecheck/tests/coverage/security). В конце сформулируй в чат вопрос как подтверждение: "Оставляем дефолт или что-то убрать/заменить?" — не предлагай несколько пакетов нагрузки. Профили -Fast/-Full можно описать, но дефолт должен быть Full. Любую предложенную команду/утилиту/флаг сопровождай кратким объяснением (1–2 предложения: что делает и зачем в CI). Не используй термины без расшифровки.
- Перед финальным `CI_TODO.md` составь таблицу всех стадий из `AgentEnforcer2/docs/ru/STAGES.md`. Для каждой стадии укажи: implement/skip/not_applicable_(с причиной). `Coverage` обязана быть в пайплайне всегда; если тестов/кода нет — `skip` с причиной и планом включения; если политика порогов не выбрана — используй безопасный дефолт без fail-порогов (TODO на политику).
- Если есть субмодули: содержимое субмодулей не читать, но `.gitmodules` можно читать только чтобы получить `path` и исключить эти пути из всех file-enumeration/hashing. CI должна игнорировать гитмодули и уважать `git ls-files`.
- Включи в `CI_TODO` ссылку-упоминание на AE2.
- Включи в `CI_TODO` предлагаемые в AE2 пороги CI инструментов.
```

#### Чат 2

```
Реализуй `CI_TODO.md`
```

### Для AI-агентов

#### Порядок чтения

| Приоритет | Документ | Зачем читать |
|-----------|----------|--------------|
| 1 | [CONCEPT.md](docs/ru/CONCEPT.md) | Философия и "почему это нужно" |
| 2 | [ARCHITECTURE.md](docs/ru/ARCHITECTURE.md) | Трехуровневая структура |
| 3 | [STAGES.md](docs/ru/STAGES.md) | Контракты этапов (stages) |
| 4 | [CACHING.md](docs/ru/CACHING.md) | Пропуск лишней работы (кэширование) |
| 5 | [templates/](templates/) | Примеры реализации |
| Опционально | [HEARTBEAT.md](docs/ru/HEARTBEAT.md) | Для долгих этапов (watchdog) |
| Опционально | [PROFILES.md](docs/ru/PROFILES.md) | Для сложных проектов |

#### Что вы должны сделать

1. **Проанализировать** целевой проект (языки, существующие инструменты, структура папок).
2. **Составить черновик** `CI_TODO.md` с описанием того, что и где будет реализовано.
3. **Предложить** матрицу инструментов (форматтеры, линтеры, проверка типов, тесты, покрытие, безопасность) и спросить пользователя, какие из них использовать.
4. **Получить подтверждение**.
5. **Создать** `run.ps1` — тонкая обертка с валидацией флагов.
6. **Создать** `build.ps1` — оркестратор с этапами для языков проекта.
7. **Создать** `build.<lang>` — логика, специфичная для языка.
8. **Добавить** `.ci_cache/` и `.enforcer/` в `.gitignore`.
9. `./run.ps1` 🙂

#### Самопроверка CI (CI Self-Check)

- Включите этап `self-check` в самом начале, чтобы проверить сам слой CI (скрипты, конфиги, связующий код). Для CI на PowerShell это обычно включает диагностику парсера и PSScriptAnalyzer.
- Подключите `self-check` к IDE пользователя.

`.vscode/settings.json`:
```json
{
  "powershell.scriptAnalysis.enable": true,
  "powershell.scriptAnalysis.settingsPath": "${workspaceFolder}/PSScriptAnalyzerSettings.psd1"
}
```

#### Инструкции для агента: гарантируйте запуск CI

Если ваш проект использует файл инструкций для агента (обычно `AGENTS.md`), он должен содержать явную команду "финальной верификации", которую агент обязан выполнить перед тем, как представить результат.

Пример (Windows / PowerShell runner):

```
./run.ps1 -Fast -SkipLaunch
```

Эта одна строка кардинально повышает надёжность цикла работы агента, так как предотвращает ответы в духе "выглядит нормально" без реального прогона CI.

Примечание: этот blueprint не предписывает, как писать `AGENTS.md`. Подробные соглашения и политику промптинга см. в [AgentCompass](https://github.com/Artemonim/AgentCompass).

## Заметки о платформе

Скрипты-примеры в этом blueprint написаны для PowerShell на Windows, но архитектура не зависит от платформы.

**Почему PowerShell (вместо Make)?**
- PowerShell — практичный стандарт для Windows-машин разработчиков.
- Он хорош в системной оркестрации: запуск процессов, настройка окружения, работа с файловой системой и структурированный вывод.
- С [PowerShell 7](https://github.com/PowerShell/PowerShell) он может быть кроссплатформенным (Windows/Linux/macOS), сохраняя единый язык скриптов.

**Когда Make/Taskfile/just подходят лучше**
- Unix-first проекты, где `make` уже является стандартом команды.
- Репозитории, предпочитающие декларативные раннеры императивным скриптам.
- Вы всё равно должны придерживаться той же трехуровневой идеи: тонкая точка входа → оркестратор → инструменты языка.

**Экосистемные раннеры (рекомендуемые точки интеграции)**
- Node/TS: npm scripts (`package.json`), Nx/Turborepo, `eslint`/`prettier`/`vitest`
- Java/Kotlin: Gradle (`gradlew`), Detekt, Spotless, тесты
- Rust: `cargo` (fmt/clippy/test), `cargo llvm-cov`
- .NET: `dotnet format`, `dotnet test`, анализаторы
- Go: `go test`, `golangci-lint`, `govulncheck`

> Во всех случаях сохраняйте ту же трехуровневую структуру и переопределяйте раннер/оркестратор в вашей любимой оболочке, если это необходимо.

## Ключевые принципы

1. **Трехуровневая архитектура**: `run.ps1` (тонкая обертка) → `build.ps1` (оркестратор) → `build.<lang>` / `tools/ci/*` (логика языка).
2. **Fail Fast, Stay Idempotent**: Хеш-кэширование, четкие границы этапов, детерминированное поведение.
3. **Наблюдаемое выполнение**: Мониторинг "пульса" (heartbeat), детектирование зависаний, структурированное логирование.
4. **Единая отчётность**: Единый формат JSON-отчёта для всех языков и этапов.

## Документация

| Документ | Описание |
|----------|----------|
| [CONCEPT.md](docs/ru/CONCEPT.md) | Философия и обоснование дизайна |
| [ARCHITECTURE.md](docs/ru/ARCHITECTURE.md) | Трехуровневая структура и поток данных |
| [STAGES.md](docs/ru/STAGES.md) | Определения этапов, статусы и контракты |
| [CACHING.md](docs/ru/CACHING.md) | Хеш-кэширование и метки доверия |
| [HEARTBEAT.md](docs/ru/HEARTBEAT.md) | Паттерны Watchdog для обнаружения зависаний |
| [REPORT_FORMAT.md](docs/ru/REPORT_FORMAT.md) | JSON-схема отчёта CI и использование |
| [PROFILES.md](docs/ru/PROFILES.md) | Профили выполнения (fast, full, security) |

## Шаблоны (Templates)

Реализации-примеры (адаптируйте, а не копируйте):

| Шаблон | Описание |
|--------|----------|
| [run.ps1](templates/run.ps1) | Скелет тонкой обертки |
| [build.ps1](templates/build.ps1) | Скелет оркестратора |
| [build.py](templates/build.py) | Логика для Python |
| [build.rs](templates/build.rs) | Логика для Rust |
| [build.ts](templates/build.ts) | Логика для TypeScript |

### Опционально (рекомендуется): CI-звуки

Можно добавить **аудиомаркеры** течения и завершения локального CI:
- Маркер "успех" при успешном завершении CI (когда используется `-SkipLaunch`)
- Маркер "провал" при первой ошибке
- Маркер "launch" при запуске приложения после завершения CI

В этом blueprint есть референс-реализация в `templates/build.ps1` и ассеты в `assets/ci_sounds` (Opus).

**Требования**:
- Плеер в PATH: `ffplay`, `mpv` или `vlc`.

**Как отключать** (намеренно не через 1-флаг):
- Удалить `assets/ci_sounds/*`.

> Лучше не добавлять в CI флаг `-Mute`. LLM начнут использовать его "чтобы не тревожить пользователя", хотя пользователь хочет быть "потревоженным" по факту завершения/провала CI.

## Схема (Schema)

| Файл | Описание |
|------|----------|
| [ci_report.schema.json](schema/ci_report.schema.json) | JSON Schema для отчётов CI |
| [ci_report.example.json](schema/ci_report.example.json) | Пример отчёта |

## Краткая справка

### Статусы этапов

| Статус | Значение | Поведение выхода |
|--------|----------|------------------|
| `ok` | Этап пройден успешно | Продолжить |
| `warn` | Этап пройден с предупреждениями | Продолжить |
| `fail` | Этап провален | Остановить пайплайн (если не настроено иное) |
| `cached` | Этап пропущен из-за попадания в кэш | Продолжить |
| `skip` | Этап пропущен (флаги / профиль / не применимо) | Продолжить |

### Типичный порядок этапов

```
self-check → fmt → lint → compile → build → test → coverage → e2e → security → launch → archive
```

### Минимальный чеклист реализации

- [ ] `run.ps1` валидирует флаги и передает управление в `build.ps1`
- [ ] `build.ps1` оркестрирует этапы с отслеживанием статусов
- [ ] Языко-специфичная логика живёт в `build.<lang>` или `tools/<lang>-ci`
- [ ] Каждый этап выдает статус `ok|warn|fail|cached|skip`
- [ ] Финальный отчёт записывается в `.ci_cache/report.json`
- [ ] `Enforcer_last_check.log` и `Enforcer_stats.log` записываются в `.enforcer/`
- [ ] Директория кэша (`.ci_cache/`) и логов (`.enforcer/`) добавлены в `.gitignore`

## Происхождение

Этот blueprint извлечён из продакшн CI-систем, используемых в:
- **[Living Layers](https://livinglayers.ru/?utm_source=github&utm_medium=organic&utm_campaign=agentenforcer2&utm_content=readme.md)**
    - Видеоигра на кастомном движке Rust поверх [Bevy](https://github.com/bevyengine/bevy)
    - 4500+ строк CI-оркестрации на `build.ps1`
- **[TelegramBot01](https://github.com/Artemonim/portfolio-mock-TelegramBot01)**
    - Python Telegram бот
    - 1200+ строк CI

## Лицензия

MIT License. Используйте эти паттерны свободно в своих проектах.

## Участие в разработке

Переводы приветствуются! Если вы хотите перевести Agent Enforcer 2 на другой язык, пожалуйста, отправьте Pull Request.

---

*Часть экосистемы [Artemonim's Agent Tools](https://github.com/Artemonim/AgentTools).*
